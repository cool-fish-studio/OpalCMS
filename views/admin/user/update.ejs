<% locals.menu = 'admin_tag'; %>
<% include ../includes/content_header %>
<div class="card-box">
    <div class="card-title">
        <div class="row">
            <div class="col-sm-6">
                标签管理
            </div>
            <div class="col-sm-6">
                <div class="button-list text-right">
                    <a class="btn btn-create" href="/admin/tag">
                        <i class="icon-reply"></i>返回列表
                    </a>
                </div>
            </div>
        </div>
    </div>
    <div class="card-content">
        <form action="/admin/tag/<%= locals.context.tag.id %>/update" method="POST">
            <div class="row form-panel">
                <div class="col-sm-6">
                    <input class="form-control" type="text" placeholder="请输入标签名称" name="name" value="<%= locals.context.tag.name || '' %>">
                    <input class="form-control" type="text" placeholder="请输入标签权重" name="weight" value="<%= locals.context.tag.weight || '' %>">
                    <div class="form-input clearfix">
                        <button class="btn-submit fr" type="submit">修改标签</button>
                    </div>
                </div>
                <div class="col-sm-6">
                    <div class="form-description">
                        说实话实话实说
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>
<% include ../includes/content_footer %>