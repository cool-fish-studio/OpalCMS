<% locals.menu = 'admin_link'; %>
<% include ../includes/content_header %>
<div class="card-box">
    <div class="card-title">
        <div class="row">
            <div class="col-sm-6">
                友情链接管理
            </div>
            <div class="col-sm-6">
                <div class="button-list text-right">
                    <a class="btn btn-create" href="/admin/link/create">
                        <i class="icon-magic"></i>创建链接
                    </a>
                </div>
            </div>
        </div>
    </div>
    <div class="card-content">
        <div class="content-list">
            <table class="table">
                <thead>
                    <tr>
                        <th>权重</th>
                        <th>名称</th>
                        <th>网址</th>
                        <th>创建时间</th>
                        <th class="actions">管理</th>
                    </tr>
                </thead>
                <tbody>
                    <% 
                        var links = locals.context.links.list || [];
                        links.forEach(function (link)
                        { 
                    %>
                    <tr>
                        <td><%- link.weight %></td>
                        <td><%- link.name %></td>
                        <td><%- locals.viewHelper.dateFormat(link.createdAt, 'yyyy-MM-dd hh:mm') %></td>
                        <td class="actions">
                            <a href="/admin/link/<%- link.id %>/update" class="text-update"><i class="icon-pencil"></i></a>
                            <a href="/admin/link/<%- link.id %>/remove" class="text-remove"><i class="icon-trash"></i></a>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
        <%- include('../includes/pagination', {'url': '/admin/link', 'pagination': locals.context.links.meta}) %>
    </div>
</div>
<% include ../includes/content_footer %>